/**
 * Weather overlay on the globe.
 * Displays VFR index markers at waypoint positions when a simulation is active.
 * Placeholder — will be connected to simulation data.
 */
export default function WeatherOverlay() {
  return null;
}
